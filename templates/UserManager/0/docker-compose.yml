version: '2'
services:
  php-nginx-user:
    image: 192.168.11.249:80/test/php-nginx-user:0.0.1
    stdin_open: true
    tty: true
    links:
    - mysql-user
    depends_on:
    - mysql-user
    restart: always
    environment: 
    - MYSQL_ROOT_PASSWORD=${mysql_root_password}
  mysql-user:
    image: 192.168.11.249:80/test/mysql-user:0.0.1
    stdin_open: true
    tty: true
    ports:
    - "${mysql_port}:3306"
    restart: always
    environment: 
    - MYSQL_ROOT_PASSWORD=${mysql_root_password}
    
  php-lb:
    image: rancher/lb-service-haproxy:v0.7.5
    ports:
      - ${public_port}:${public_port}
  
